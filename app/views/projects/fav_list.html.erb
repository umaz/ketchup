<h1>お気に入り</h1>
<table class="table table-striped">
  <tr>
    <th>お気に入り一覧</th>
  </tr>
  <tr>
    <% @lists.each do |key, val| %>
    <td>
      <%= link_to detail_path(key) do %>
        <%= val %>
      <% end %>
    </td>
  </tr>
  <% end %>
</table>
